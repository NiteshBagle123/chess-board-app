{
  "name": "chess-app",
  "version": "1.0.0",
  "description": "chess gaming app",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage",
    "eslint": "eslint --fix src/**/*.{ts,js,tsx}",
    "format": "prettier --write src/**/*.{ts,js,tsx}"
  },
  "author": "Nitesh Bagle",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.1",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm run test"
    }
  },
  "lint-staged": {
    "*.{ts,js,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
